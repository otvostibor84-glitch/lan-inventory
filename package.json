{
  "name": "lan-inventory",
  "version": "0.1.0",
  "private": true,
  "description": "Switch, port, VLAN and MAC address inventory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
